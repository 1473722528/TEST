import DialogFormComponents from './DialogForm.vue'



const  DialogForm= {
    install: function (Vue) {
        Vue.component('DialogForm',  DialogFormComponents)
    }
}

export default  DialogForm;